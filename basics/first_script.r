#Creating the first Script

#Assing a value to the variable
msg <- 'Hello World'

#use the command 'print()'
print(msg)

#To learn more about the functions, use '?' and then the function on R terminal
#Example: '?print'